<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Muhammad Sufyan — Hero Preview</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;700&family=Inter:wght@400;500&display=swap');

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html, body {
    height: 100%;
    background: #0B0E14;
    font-family: 'Inter', sans-serif;
    overflow: hidden;
  }

  .hero {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(ellipse at 50% 30%, #10141C 0%, #0B0E14 70%);
  }

  canvas {
    position: absolute;
    top: 0; left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .content {
    position: relative;
    z-index: 2;
    text-align: center;
    padding: 0 24px;
  }

  .eyebrow {
    font-size: 13px;
    letter-spacing: 3px;
    color: #38BDF8;
    text-transform: uppercase;
    margin-bottom: 20px;
    font-weight: 500;
  }

  h1 {
    font-family: 'Space Grotesk', sans-serif;
    font-size: clamp(40px, 7vw, 76px);
    font-weight: 700;
    color: #E7EAF0;
    letter-spacing: -1px;
    line-height: 1.05;
    margin-bottom: 20px;
  }

  h1 span {
    color: #38BDF8;
    text-shadow: 0 0 24px rgba(56, 189, 248, 0.45);
  }

  .tagline {
    font-size: 17px;
    color: #8891A5;
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: 1.6;
  }

  .cta-row {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    padding: 14px 28px;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 500;
    font-family: 'Inter', sans-serif;
    cursor: pointer;
    transition: all 0.25s ease;
    text-decoration: none;
    display: inline-block;
  }

  .btn-primary {
    background: #38BDF8;
    color: #0B0E14;
    border: 1px solid #38BDF8;
    box-shadow: 0 0 20px rgba(56, 189, 248, 0.35);
  }

  .btn-primary:hover {
    box-shadow: 0 0 30px rgba(56, 189, 248, 0.6);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: transparent;
    color: #E7EAF0;
    border: 1px solid #2A3142;
  }

  .btn-secondary:hover {
    border-color: #38BDF8;
    color: #38BDF8;
  }

  .social-row {
    margin-top: 44px;
    display: flex;
    gap: 24px;
    justify-content: center;
  }

  .social-row a {
    color: #8891A5;
    font-size: 13px;
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: all 0.2s ease;
  }

  .social-row a:hover {
    color: #38BDF8;
    border-bottom-color: #38BDF8;
  }

  .note {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    color: #4A5266;
    font-size: 12px;
    z-index: 2;
    letter-spacing: 0.5px;
  }
</style>
</head>
<body>

<div class="hero">
  <canvas id="net"></canvas>
  <div class="content">
    <div class="eyebrow">Data Science Enthusiast &middot; ML Engineer</div>
    <h1>Muhammad <span>Sufyan</span></h1>
    <p class="tagline">Turning data into actionable insights — currently building intelligent systems at FlyRank AI.</p>
    <div class="cta-row">
      <a href="#" class="btn btn-primary">View Projects</a>
      <a href="#" class="btn btn-secondary">Download Resume</a>
    </div>
    <div class="social-row">
      <a href="#">GitHub</a>
      <a href="#">LinkedIn</a>
      <a href="#">Email</a>
    </div>
  </div>
  <div class="note">Preview — dark natural background + neon blue neural connection animation</div>
</div>

<script>
const canvas = document.getElementById('net');
const ctx = canvas.getContext('2d');
let w, h, nodes, pulses = [];

function resize() {
  w = canvas.width = window.innerWidth;
  h = canvas.height = window.innerHeight;
}
window.addEventListener('resize', resize);
resize();

const NODE_COUNT = 55;
const LINK_DIST = 150;

function initNodes() {
  nodes = [];
  for (let i = 0; i < NODE_COUNT; i++) {
    nodes.push({
      x: Math.random() * w,
      y: Math.random() * h,
      vx: (Math.random() - 0.5) * 0.25,
      vy: (Math.random() - 0.5) * 0.25,
      r: Math.random() * 1.5 + 1
    });
  }
}
initNodes();

function maybeSpawnPulse(links) {
  if (Math.random() < 0.02 && links.length > 0) {
    const link = links[Math.floor(Math.random() * links.length)];
    pulses.push({ a: link.a, b: link.b, t: 0 });
  }
}

function draw() {
  ctx.fillStyle = '#0B0E14';
  ctx.fillRect(0, 0, w, h);

  const links = [];

  for (let i = 0; i < nodes.length; i++) {
    const n = nodes[i];
    n.x += n.vx;
    n.y += n.vy;
    if (n.x < 0 || n.x > w) n.vx *= -1;
    if (n.y < 0 || n.y > h) n.vy *= -1;
  }

  for (let i = 0; i < nodes.length; i++) {
    for (let j = i + 1; j < nodes.length; j++) {
      const a = nodes[i], b = nodes[j];
      const dx = a.x - b.x, dy = a.y - b.y;
      const dist = Math.sqrt(dx * dx + dy * dy);
      if (dist < LINK_DIST) {
        const opacity = (1 - dist / LINK_DIST) * 0.35;
        ctx.strokeStyle = `rgba(56, 189, 248, ${opacity})`;
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(a.x, a.y);
        ctx.lineTo(b.x, b.y);
        ctx.stroke();
        links.push({ a, b });
      }
    }
  }

  maybeSpawnPulse(links);

  pulses = pulses.filter(p => p.t < 1);
  pulses.forEach(p => {
    p.t += 0.02;
    const x = p.a.x + (p.b.x - p.a.x) * p.t;
    const y = p.a.y + (p.b.y - p.a.y) * p.t;
    ctx.beginPath();
    ctx.arc(x, y, 2.5, 0, Math.PI * 2);
    ctx.fillStyle = 'rgba(125, 211, 252, 0.9)';
    ctx.shadowBlur = 12;
    ctx.shadowColor = '#7DD3FC';
    ctx.fill();
    ctx.shadowBlur = 0;
  });

  nodes.forEach(n => {
    ctx.beginPath();
    ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
    ctx.fillStyle = 'rgba(56, 189, 248, 0.75)';
    ctx.shadowBlur = 6;
    ctx.shadowColor = '#38BDF8';
    ctx.fill();
    ctx.shadowBlur = 0;
  });

  requestAnimationFrame(draw);
}
draw();
</script>

</body>
</html>